import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.accenture.todo',
  appName: 'to-do-accenture',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
